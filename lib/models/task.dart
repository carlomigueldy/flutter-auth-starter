class Task {
  int id;
  String title;
  String description;
  String date;
  bool isCompleted;

  Task({
    this.id,
    this.title,
    this.description,
    this.date,
    this.isCompleted,
  });
}
